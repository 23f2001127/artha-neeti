"""filings-rag-mcp: RAG pipeline (ingest + retrieve) over Indian annual-report PDFs.

Ingestion (`ingest.py`) is a run-once/on-demand CLI. The MCP server (`server.py`)
exposes the live query tools for the Filings Agent.
"""
